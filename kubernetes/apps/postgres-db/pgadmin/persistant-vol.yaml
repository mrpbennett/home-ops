apiVersion: v1
kind: PersistentVolume
metadata:
  name: pgadmin-volume
  labels:
    type: local
    app: pgadmin
spec:
  storageClassName: manual
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /mnt/storage/pgadmin
