apiVersion: v1
kind: Secret
metadata:
  name: admin-talos
  namespace: kubernetes-dashboard
  annotations:
    kubernetes.io/service-account.name: "admin-talos"
type: kubernetes.io/service-account-token
