apiVersion: "app.redislabs.com/v1"
kind: "RedisEnterpriseCluster"
metadata:
  name: redis-prod-cluster
spec:
  nodes: 3

  username: pbennett.uk@gmail.com

  redisEnterpriseNodeResources:
    limits:
      cpu: 500m # half a core
      memory: 2Gi
    requests:
      cpu: 250m
      memory: 1Gi
