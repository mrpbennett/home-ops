apiVersion: v1
kind: Service
metadata:
  name: redis-stack
spec:
  selector:
    app: redis
  ports:
    - name: redis-port
      protocol: TCP
      port: 6379
      targetPort: 6379
    - name: ui-port
      protocol: TCP
      port: 8001
      targetPort: 8001
  type: LoadBalancer
