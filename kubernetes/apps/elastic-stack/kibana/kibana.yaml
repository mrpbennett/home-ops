apiVersion: kibana.k8s.elastic.co/v1
kind: Kibana
metadata:
  name: kibana
spec:
  version: 8.15.3
  count: 1

  elasticsearchRef:
    name: es70ld
    namespace: elastic-stack

  config:
    server.publicBaseUrl: https://kibana.70ld.home

  http:
    service:
      spec:
        type: ClusterIP
