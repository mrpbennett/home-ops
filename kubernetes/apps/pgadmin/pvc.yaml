kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pgadmin-pvc
  labels:
    app: pgadmin
spec:
  storageClassName: longhorn-static
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
