apiVersion: v1
kind: Service
metadata:
  name: docker-registry-svc
spec:
  annotations:
    kube-vip.io/loadbalancerIPs: 192.168.5.100
  type: LoadBalancer
  selector:
    app: docker-registry
  ports:
    - port: 5000
      targetPort: 5000
