apiVersion: v1
kind: Service
metadata:
  name: docker-registry-svc
spec:
  type: LoadBalancer
  loadBalancerIP: 192.168.7.210
  selector:
    app: docker-registry
  ports:
    - port: 5000
      targetPort: 5000
