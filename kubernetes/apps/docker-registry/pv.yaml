kind: PersistentVolume
apiVersion: v1
metadata:
  name: docker-registry-pv
  labels:
    app: docker-registry
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /var/lib/longhorn/docker-registry
