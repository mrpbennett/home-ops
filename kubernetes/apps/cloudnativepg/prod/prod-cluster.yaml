apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: cnpg-cluster-prod
  namespace: cnpg-cluster-prod
spec:
  description: "70LD PostgreSQL PROD Cluster"
  instances: 3
  startDelay: 3600
  stopDelay: 300
  primaryUpdateStrategy: unsupervised

  probes:
    startup:
      initialDelaySeconds: 30
      periodSeconds: 10
      timeoutSeconds: 5

  projectedVolumeTemplate:
    sources: []

  postgresql:
    parameters:
      shared_buffers: "256MB"

  resources:
    requests:
      memory: "1Gi"
      cpu: 1
    limits:
      memory: "1Gi"
      cpu: 1

  storage:
    storageClass: longhorn
    size: 10Gi

  bootstrap:
    initdb:
      database: main
      owner: paul
      secret:
        name: cnpg-cluster-prod-user

  enableSuperuserAccess: true
  superuserSecret:
    name: cnpg-cluster-prod-superuser

  managed:
    roles:
      # Role: My personal user
      - name: paul
        ensure: present
        comment: "Paul's CNPG Cluster User"
        login: true
        superuser: true
        passwordSecret:
          name: cnpg-cluster-user

      # Role: Authentik user
      - name: authentik
        ensure: present
        comment: "Simple application user for Authentik"
        login: true
        superuser: true
        passwordSecret:
          name: cnpg-authentik-user

      # Role: Lakekeeper user
      - name: lakekeeper
        ensure: present
        comment: "Lakekeeper DB for managing data lakes"
        login: true
        superuser: true
        passwordSecret:
          name: cnpg-lakekeeper-user

      # Role: Harbor user
      - name: harbor
        ensure: present
        comment: "Harbor DB for managing registry"
        login: true
        superuser: true
        passwordSecret:
          name: cnpg-harbor-user

    services:
      additional:
        - selectorType: rw
          serviceTemplate:
            metadata:
              name: postgres-prod-rw-svc
              annotations:
                metallb.universe.tf/loadBalancerIPs: 192.168.7.80
            spec:
              type: LoadBalancer

  # backup:
  #   retentionPolicy: "7d"
  #   barmanObjectStore:
  #     destinationPath: s3://cnpg-cluster
  #     endpointURL: https://minio-tenant-hl.minio-tenant.svc.cluster.local:9000
  #     s3Credentials:
  #       accessKeyId:
  #         name: cnpg-cluster-minio
  #         key: MINIO_ACCESS_KEY
  #       secretAccessKey:
  #         name: cnpg-cluster-minio
  #         key: MINIO_SECRET_KEY

  affinity:
    enablePodAntiAffinity: true
    topologyKey: failure-domain.beta.kubernetes.io/zone

  monitoring:
    enablePodMonitor: true
