apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: cnpg-cluster-prod-backup
  namespace: cnpg-cluster-prod
spec:
  schedule: "0 0 0 * * *"
  backupOwnerReference: cluster
  suspend: true
  cluster:
    name: cnpg-cluster
