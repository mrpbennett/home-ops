apiVersion: v1
kind: Service
metadata:
  name: homepage
  namespace: homepage-dashboard
  labels:
    app.kubernetes.io/name: homepage
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: homepage
  ports:
    - port: 3000
      # targetPort: http
      # protocol: TCP
      # name: http
