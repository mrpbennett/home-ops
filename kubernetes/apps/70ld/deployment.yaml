apiVersion: apps/v1
kind: Deployment
metadata:
  name: 70ld-site
spec:
  replicas: 1
  selector:
    matchLabels:
      app: 70ld-site
  template:
    metadata:
      labels:
        app: 70ld-site
    spec:
      containers:
        - name: 70ld-site
          image: 192.168.7.210:5000/70ld-site:0.0.1
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: 70ld-site-svc
spec:
  selector:
    app: 70ld-site
  ports:
    - port: 80
      targetPort: 80
