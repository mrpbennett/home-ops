apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: coder-db-pvc
  namespace: coder
  labels:
    app: coder
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
