apiVersion: v1
kind: PersistentVolume
metadata:
  name: coder-db-pv
  namespace: coder
  labels:
    type: local
    app: coder
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /mnt/storage/coder-db
