apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agrocd-ts-ingress
  namespace: argocd
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: argocd-server
      port:
        number: 80
  tls:
    - hosts:
        - argocd
