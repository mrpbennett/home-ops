apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homepage-ts-ingress
  namespace: homepage
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: homepage
      port:
        number: 80
  tls:
    - hosts:
        - homepage.home.local
