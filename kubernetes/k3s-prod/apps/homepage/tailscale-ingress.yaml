apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homepage-ts-ingress
  namespace: homepage
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: homepage-tailscale-svc
      port:
        name: https
---
apiVersion: v1
kind: Service
metadata:
  name: homepage-tailscale-svc
spec:
  type: ClusterIP
  ports:
    - name: https
      port: 443
      targetPort: 443
