xxxx


Pick your VIP ahead of time

# FIRST SERVER
curl -sfL https://get.k3s.io | K3S_TOKEN=xxxx sh -s - server \
    --cluster-init \
    --disable=traefik \
    --disable=servicelb \
    --tls-san=192.168.6.200 # Optional, needed if using a fixed registration address


# ADDIONTAL SERVER
curl -sfL https://get.k3s.io | K3S_TOKEN=xxxx sh -s - server \
    --server https://192.168.6.1:6443 \
    --disable=traefik \
    --disable=servicelb \
    --tls-san=192.168.6.200 # Optional, needed if using a fixed registration address


# /etc/rancher/k3s/config.yaml
token: lb-cluster-gd
tls-san: 10.10.10.100

# AGENT
curl -sfL https://get.k3s.io | K3S_TOKEN=xxxx sh -s - agent --server https://192.168.6.1:6443

# KUBE CONF

sudo cat /etc/rancher/k3s/k3s.yaml



