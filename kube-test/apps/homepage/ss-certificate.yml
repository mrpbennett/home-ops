apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ss-certificate
  namespace: homepage
spec:
  secretName: ss-secret
  issuerRef:
    kind: ClusterIssuer
    name: ss-cluster-issuer
  dnsNames:
  - 'homepage.pnfb.home'