apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard-cert
  namespace: cert-manager
spec:
  dnsNames: ['*.pnfb.uk']
  secretName: letsencrypt-tls
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt